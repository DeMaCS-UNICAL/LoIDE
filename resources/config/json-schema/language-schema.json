{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "$id": "language-schema.json",
    "type": "object",
    "properties": {
        "language": {
            "title": "Language name.",
            "description": "This property indicates the language's name.",
            "type": "string"
        },
        "version": {
            "title": "Language version.",
            "description": "This property indicates the language version.",
            "type": "string"
        },
        "syntax-highlighter": {
            "title": "Syntax highlighter URL.",
            "description": "This property indicates the syntax highlighter URL corresponding to this language.",
            "type": "object",
            "properties": {
                "protocol": {
                    "title": "Protocol name.",
                    "description": "This property indicates the protocol name to use for the connection.",
                    "enum": [ "ws", "http" ]
                },
                "url": {
                    "title": "Syntax-Highlighter URL.",
                    "description": "This property indicates the syntax-highlighter url.",
                    "type": "string",
                    "oneOf": [
                        { "format": "hostname" },
                        { "format": "ipv4" },
                        { "format": "ipv6" }
                    ]
                },
                "path": {
                    "title": "Missing.",
                    "description": "Missing.",
                    "type": "string",
                    "_comment": "Add the pattern to match with the 'path'."
                },
                "port": {
                    "title": "Connection port number.",
                    "description": "This property indicates the port number.",
                    "type": "number",
                    "minimum": 0,
                    "maximum": 65535
                }
            },
            "required": [
                "protocol",
                "url",
                "path",
                "port"
            ],
            "additionalProperties": false
        },
        "solvers": {
            "title": "missing",
            "description": "missing",
            "type": "array",
            "minItems": 1,
            "items": {
                "$ref": "solver-schema.json"
            }
        }
    },
    "required": [
        "language",
        "version",
        "solvers"
    ],
    "additionalProperties": false
}