{
    "$schema": "http://json-schema.org/draft-06/schema#",
    // TODO need to choose the proper id
    "$id": "http://localhost:8080/resources/config/json-schema/app-config-schema.json",
    "type": "object",
    "properties": {
        "port": {
            "title": "Connection port of the LoIDE Web Application.",
            "description": "This property specifies the port to use for the LoIDE Web Application.",
            "type": "number",
            "minimum": 0,
            "maximum": 65535
        }
    },
    "required": [
        "port"
    ],
    "additionalProperties": false    
}